dist
node_modules
package-lock.json

# ai-pr-agent.yml is generated by homelab-infra's terraform/github/proklinator-app and
# overwritten on every apply, so it can never be formatted here — reformatting it would
# be undone by the next `just deploy github proklinator-app`.
.github