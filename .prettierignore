dist
node_modules
package-lock.json

# ai-pr-agent.yml, ai-pr-review.yml and ai-issue-agent.yml are generated by homelab-infra's
# terraform/github/proklinator-app and overwritten on every apply, so they can never be
# formatted here — reformatting them would be undone by the next
# `just deploy github proklinator-app`.
.github